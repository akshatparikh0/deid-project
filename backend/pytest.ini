[pytest]
# Scoped to redaction_pipeline's own tests — the Django app (documents/,
# accounts/) uses `python manage.py test` instead (see .github/workflows/ci.yml),
# since it needs Django's test database/app setup, not a plain pytest run.
testpaths = redaction_pipeline/tests
